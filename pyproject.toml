[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "scrape-yahoo-odds"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cloudscraper",
    "jsonpath-ng",
    "pandas",
]
